# This project uses only public packages. Override any global private registry
# (e.g. an AWS CodeArtifact registry with an expired token) so installs work.
registry=https://registry.npmjs.org/
audit=false
fund=false
